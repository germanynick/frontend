import { UserCreateContainer } from '@frontend/features/users';
import { NextPage } from 'next';

export const UserCreatePage: NextPage = () => {
  return <UserCreateContainer />;
};

export default UserCreatePage;
