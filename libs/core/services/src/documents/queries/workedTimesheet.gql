query workedTimesheet($id: String!){
    workedTimesheet(id: $id){
        callHours
        id
        paidHours
        shiftsWorked
        status
        weekManagement{
            active
            endDate
            id
            startDate
        }
    }
}